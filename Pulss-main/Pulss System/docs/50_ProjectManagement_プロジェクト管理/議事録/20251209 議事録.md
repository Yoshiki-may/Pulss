## 2025-12-09 議事録（Codex作業サマリ）

### 要件整理・方針
- ダッシュボード/AIの機能要件書（P0/P1/P2）を精読し、営業〜CS〜運用〜効果測定までの導線と優先度を整理。アーキは現行構造（React + services）を維持し、拡張追加で対応する方針を決定。

### 実装準備・型/サービス
- `types.ts` を拡張：リード、ヒアリング記録、宿題URL、提案、契約、コンテンツカレンダー、レポート、通知など新機能用の型を追加。
- `services/dashboardService.ts` を新規作成：上記新機能のモックデータを提供し、UIの土台を表示可能に。

### 画面追加（P0土台）
- Salesボード（リード一覧/ステータス/ランク表示）。
- ヒアリング/宿題（商談メモと宿題URL一覧）。
- 提案/フォロー（提案送付状況とフォロー期日）。
- 契約/引き継ぎ（契約情報・クライアントレポート導線）。
- タスクボード（導入/運用タスク＋簡易リマインダー）。
- 投稿カレンダー（撮影→編集→確認→投稿ステータス）。
- 効果レポート（月次KPI/ハイライト、PDF/CSVリンク）。
- 通知センター（フォロー/期限/リスクを集約）。

### ナビ・ルーティング
- `Sidebar` に新規ビュー（sales/hearings/proposals/contracts/tasks/calendar/reports/notifications）を追加し、`App.tsx` の `currentView` 切替に各ページを紐づけ。

### ビルド・動作確認
- `npm run build` 成功（新規ページ/型拡張後もビルド通過）。
