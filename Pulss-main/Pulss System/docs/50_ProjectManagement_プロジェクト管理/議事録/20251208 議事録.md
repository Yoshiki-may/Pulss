# 2025-12-08 議事録

## 本日のログ状況
- `docs/50_ProjectManagement_プロジェクト管理/現状報告/会話ログ/20251208 1800 Codex.md` は 0 バイトで内容未記録。12/08 のやり取りがある場合は別途追記が必要。

## 前日（12/07）までの進捗サマリ
- フロント（`Pulss system/Code/Pulss-main`）の文字化けを修正し、日本語 UI を整備。ニュース/スケジュール/ステータス文言の再翻訳、コピー失敗フォールバックなどを実装。
- バックエンド（`backend/`）を新設し、FastAPI で `api/health` と `api/sns-news`（POST/GET/ID 取得）を追加。CORS に `http://3.107.236.7:5173` と `http://3.107.236.7:8000` を許可。
- フロントのニュース取得を API ベース URL（`.env` の `VITE_API_BASE_URL`、デフォルト `http://3.107.236.7:8000`）経由に切り替え、失敗時のみモックにフォールバック。
- `.env.example` を追加、`npm run build`（フロント）実行済みで成功を確認済み。
- `.gitignore` を ASCII コメントに修正し、不要ファイル（Obsidian/Node/Python/OS 系）を除外。`git config user.name "Yoshiki-may"`, `user.email "brainsystem@shishayuenchi.com"` を設定し初回コミット済み（push は未実施）。

## 躓いた箇所と解決策
- 文字化け（UTF-8 以外混在）：ファイルを UTF-8 に統一し、日本語文言を再入力。
- OpenAI 無料枠・レート制限：課金有効化と日次上限設定を依頼（対応待ち）。
- Python 3.9 の型注釈エラー（`|` と `Literal` 組み合わせ）：`from __future__ import annotations` と `Union[...]` に置き換えて解消。`list_sns_news` も Union へ修正。
- バックエンド起動時の再エラー：前述の型修正を main ブランチに push 済み。EC2 側は `git pull` 後に再起動で解決。

## 次にやること・注意点
- EC2 でバックエンド起動：`cd backend && source .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8000`。最新を `git pull` してから実行。
- フロント起動/ビルド確認：`cd "Pulss system/Code/Pulss-main" && npm run dev -- --host 0.0.0.0 --port 5173`（または dist を配信）。`.env` に `VITE_API_BASE_URL=http://3.107.236.7:8000` を設定しているか確認。
- 疎通確認：n8n などから `POST /api/sns-news` を送り、フロントのニュース一覧に反映されるか確認。
- GitHub 反映：`git push -u origin main`（PAT/SSH 用意済みか要確認）。会話ログなど未コミットの md を必要に応じてコミット。
- OpenAI 課金設定が未完の場合は引き続き上限を決めて有効化し、レート制限を解消する。
