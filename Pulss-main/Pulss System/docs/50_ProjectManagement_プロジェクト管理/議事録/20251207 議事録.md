# 2025-12-08 議事録

## 今日の作業まとめ
- バックエンド統合・クリーンアーキ適用  
  - `pulss_backend` サブフォルダを廃止し、`app/backend` 直下に `domain/services/infrastructure/api/app/utils/main` を配置。  
  - 相対インポート修正、`requirements.txt` を `fastapi`, `uvicorn[standard]`, `pydantic` に更新、`README` を現構成に合わせて再作成。  
  - `create_fastapi_app` 起動に一本化し、クライアント/タスク/パルス回答/ディレクターボード/AIサジェストのエンドポイントを維持。

- フロントエンド拡張（Pulss-main）  
  - Appナビにディレクターボードを追加、ClientDetailをタブ構造（概要/導入タスク/運用タスク/AI提案）へ刷新。  
  - 導入/運用タスクパネル（ステータス変更・期限表示・新規追加）、AI提案パネル（ドラフト生成/閲覧）、ディレクターボード画面を追加。  
  - `types` と `clientService` を新APIに合わせて拡張し、SNSニュースのフォールバックモックを用意。パルス要件レポートを v2 項目（強み・ブランドストーリー等）に対応。

- ビルド・確認  
  - `npm run build` (Pulss-main) を実施し成功。バックエンドはインメモリ実装のままローカル起動前提で整理。

## 主な変更ファイル
- バックエンド: `Pulss System/app/backend/{main.py,app.py,api.py,domain.py,services.py,infrastructure.py,utils.py,requirements.txt,README.md}`
- フロント: `Pulss System/app/Code/Pulss-main/{App.tsx,components/Layout/Sidebar.tsx,components/ClientDetails/ClientDetail.tsx,components/Tasks/TaskPanel.tsx,components/Ai/AiSuggestionsPanel.tsx,components/DirectorBoard/DirectorBoardPage.tsx,components/ClientDetails/PulseReport.tsx,components/ClientDetails/SnsNewsWidget.tsx,services/clientService.ts,types.ts}`

## 補足・リスク
- バックエンドはインメモリ実装のため、公開前に DB アダプタと認証を追加する必要あり。  
- AIサジェストはスタブ実装。実運用では LLM クライアントへ差し替え予定。  
- SNSニュース API はモックフォールバック頼り。実データ取得が必要ならサーバ側実装を追加する。
